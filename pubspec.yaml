name: bloc_process
description: "A package that extends the BloC pattern to fully support linear processes as well as chaining of such processes."
version: 0.3.4
repository: https://github.com/BenediktJSchlegel/bloc_process

environment:
  sdk: '>=3.2.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test:
  mockito: ^5.4.4
  build_runner: ^2.4.8


flutter: